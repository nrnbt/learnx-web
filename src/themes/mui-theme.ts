'use client'

import { createTheme } from '@mui/material/styles'

export const theme = createTheme({
  palette: {
    primary: {
      main: '#191A2C',
      dark: '#0F101C',
      light: '#2E3048',
    },
    secondary: {
      main: '#A7E628',
      dark: '#6D9A1C',
      light: '#D1F571',
    }
  }
})
